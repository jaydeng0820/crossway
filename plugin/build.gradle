subprojects {
    dependencies {
        api(
                project(':core'),
                project(':spring'),
                project(':commons'),
                project(':exception')
        )
    }
}
