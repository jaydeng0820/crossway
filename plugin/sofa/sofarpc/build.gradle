dependencies {

    compileOnly(
            project(':plugin:sofa:sofarpc-api'),
            libraries.'sofa-boot',
            libraries.'sofa-rpc'
    )

    testImplementation(
            project(':plugin:sofa:sofarpc-api'),
    )
}